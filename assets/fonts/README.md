# Fonts

`LiberationSans-Regular.ttf` / `LiberationSans-Bold.ttf` — Liberation Sans,
licensed under the **SIL Open Font License 1.1** (redistributable). Used only by
`scripts/generate-og.mjs` to render the build-time Open Graph card; not shipped
to the browser. Swap for any OFL/licensed font when reusing this generator.
